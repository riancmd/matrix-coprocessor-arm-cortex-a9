module interface(
	input clk, rst, start,
	input clk_button,
	output ready, overflow,
	output [1:0] state
);

control_unit control(
	.clk(clk),
	.rst(rst),
	.start(start),
	.clk_button(clk_button),
	.ready(ready),
	.overflow(overflow),
	.state_output(state)
);

endmodule